services:
  uptime-kuma:
    image: louislam/uptime-kuma:1
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./data:/app/data
    ports:
      - 3003:3001
    restart: unless-stopped
networks: {}
